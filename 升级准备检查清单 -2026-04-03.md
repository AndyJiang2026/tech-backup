# ✅ OpenClaw 升级准备检查清单

**升级时间**: 2026-04-03 23:50 GMT+8  
**目标版本**: 2026.3.28  
**执行状态**: ⏳ 准备中

---

## 📋 准备清单（23:45 前完成）

### 系统检查

- [ ] **确认当前版本**
  ```bash
  openclaw --version
  # 应显示：OpenClaw 2026.3.13
  ```

- [ ] **确认 Gateway 状态**
  ```bash
  openclaw gateway status
  # 应显示正常运行
  ```

- [ ] **确认磁盘空间**
  ```bash
  df -h /home
  # 至少需要 500MB 可用空间
  ```

- [ ] **确认 sudo 权限**
  ```bash
  sudo whoami
  # 应显示：root
  ```

---

### 备份检查

- [ ] **现有备份确认**
  ```bash
  ls -lh /home/admin/.openclaw/openclaw.json.bak*
  ls -lh /home/admin/.openclaw/openclaw.json-bak0328
  ```
  预期：至少 3 个备份文件

- [ ] **工作空间 Git 状态**
  ```bash
  cd /home/admin/.openclaw/workspace-chief-agent
  git status
  ```
  预期：工作区干净或有重要变更已提交

---

### 文档准备

- [ ] **升级评估报告** - ✅ 已创建
  - `/home/admin/.openclaw/workspace-chief-agent/升级评估报告 - 百炼镜像版 -2026-04-03.md`

- [ ] **升级执行计划** - ✅ 已创建
  - `/home/admin/.openclaw/workspace-chief-agent/升级执行计划 -2026-04-03.md`

- [ ] **升级脚本** - ✅ 已创建
  - `升级快速命令 -2026-04-03.sh`
  - `升级回滚脚本 -2026-04-03.sh`

---

## 🚀 升级执行（23:50 开始）

### 快速执行（推荐）

```bash
cd /home/admin/.openclaw/workspace-chief-agent
bash 升级快速命令 -2026-04-03.sh
```

### 手动执行（备选）

按照 `升级执行计划 -2026-04-03.md` 逐步执行

---

## ✅ 验证清单（升级后）

### 基础验证（必须全部通过）

- [ ] **版本号正确**
  ```bash
  openclaw --version
  # 应显示：OpenClaw 2026.3.28
  ```

- [ ] **Gateway 正常运行**
  ```bash
  openclaw gateway status
  # 应显示正常运行状态
  ```

- [ ] **配置文件格式正确**
  ```bash
  cat /home/admin/.openclaw/openclaw.json | python3 -m json.tool > /dev/null && echo "✅ 正确"
  ```

- [ ] **Gateway 进程运行**
  ```bash
  pgrep -a openclaw
  # 应显示进程信息
  ```

- [ ] **端口正常监听**
  ```bash
  netstat -tlnp | grep 16023
  # 应显示端口监听
  ```

---

### 插件验证

- [ ] **钉钉连接器**
  ```bash
  ls -la /home/admin/.openclaw/extensions/dingtalk-connector/
  ```

- [ ] **QQ 机器人**
  ```bash
  ls -la /home/admin/.openclaw/extensions/openclaw-qqbot/
  ```

- [ ] **DashScope 配置**
  ```bash
  ls -la /home/admin/.openclaw/extensions/dashscope-cfg/
  ```

---

### 数据验证

- [ ] **工作空间完整**
  ```bash
  ls -la /home/admin/.openclaw/workspace-chief-agent/ | head -20
  ```

- [ ] **记忆系统完整**
  ```bash
  ls -la /home/admin/.openclaw/memory/
  ```

- [ ] **配置文件完整**
  ```bash
  cat /home/admin/.openclaw/openclaw.json | grep -A 3 '"channels"'
  ```

---

## 🔄 回滚方案（如需）

### 触发条件

出现以下任一情况立即回滚：
- ❌ Gateway 无法启动
- ❌ 配置文件加载失败
- ❌ 核心插件无法加载
- ❌ 业务功能严重异常

### 快速回滚

```bash
cd /home/admin/.openclaw/workspace-chief-agent
bash 升级回滚脚本 -2026-04-03.sh
```

### 手动回滚

```bash
# 1. 停止 Gateway
openclaw gateway stop

# 2. 恢复配置
sudo cp /home/admin/.openclaw/openclaw.json.backup.20260403.2350 \
  /home/admin/.openclaw/openclaw.json

# 3. 回滚版本
sudo npm install -g openclaw@2026.3.13

# 4. 重启 Gateway
openclaw gateway restart
```

---

## 📞 问题排查

### 查看日志

```bash
# Gateway 日志
openclaw logs --tail 100

# 系统日志
journalctl -u openclaw -n 50 2>/dev/null || tail -50 /home/admin/.openclaw/logs/*.log
```

### 配置检查

```bash
# JSON 格式验证
cat /home/admin/.openclaw/openclaw.json | python3 -m json.tool

# 插件配置
cat /home/admin/.openclaw/openclaw.json | grep -A 10 '"plugins"'
```

---

## 📊 升级时间线

| 时间 | 事件 | 状态 |
|------|------|------|
| 23:40 | 准备提醒 | ⏳ 待触发 |
| 23:45 | 升级前检查 | ⏳ 待执行 |
| 23:50 | 开始升级 | ⏳ 待执行 |
| 23:53 | 备份完成 | ⏳ 待执行 |
| 23:58 | 升级完成 | ⏳ 待执行 |
| 00:01 | 重启 Gateway | ⏳ 待执行 |
| 00:10 | 验证完成 | ⏳ 待执行 |

---

## 📝 升级记录

**升级开始时间**: _______________  
**升级完成时间**: _______________  
**实际耗时**: _______________  

**遇到的问题**:
```
_________________________________
_________________________________
```

**解决方案**:
```
_________________________________
_________________________________
```

**验证结果**:
- 基础验证：□ 通过 / □ 失败
- 插件验证：□ 通过 / □ 失败
- 数据验证：□ 通过 / □ 失败

**总体评价**: □ 成功 / □ 部分成功 / □ 失败

---

## 🎯 关键文件位置

| 文件 | 位置 |
|------|------|
| **升级脚本** | `/home/admin/.openclaw/workspace-chief-agent/升级快速命令 -2026-04-03.sh` |
| **回滚脚本** | `/home/admin/.openclaw/workspace-chief-agent/升级回滚脚本 -2026-04-03.sh` |
| **执行计划** | `/home/admin/.openclaw/workspace-chief-agent/升级执行计划 -2026-04-03.md` |
| **备份文件** | `/home/admin/.openclaw/openclaw.json.backup.20260403.2350` |
| **配置文件** | `/home/admin/.openclaw/openclaw.json` |

---

**准备完成度**: ⏳ 等待 23:45 执行检查  
**最后检查时间**: 2026-04-03 14:56 GMT+8
