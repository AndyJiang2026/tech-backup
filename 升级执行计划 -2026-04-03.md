# 🚀 OpenClaw 升级执行计划

**升级时间**: 2026-04-03 23:50 GMT+8  
**目标版本**: 2026.3.28（百炼镜像基准）  
**执行方案**: 方案 A - 保守安全升级  
**预计耗时**: 15-20 分钟

---

## 📋 升级前检查（23:45 执行）

### 1. 系统状态检查

```bash
# 当前版本
openclaw --version

# Gateway 状态
openclaw gateway status

# 磁盘空间（至少需要 500MB）
df -h /home

# 当前时间
date
```

### 2. 备份验证

```bash
# 检查现有备份
ls -lh /home/admin/.openclaw/openclaw.json.bak*
ls -lh /home/admin/.openclaw/openclaw.json-bak0328
```

---

## 🔧 升级执行步骤（23:50 开始）

### 步骤 1: 完整备份（23:50-23:53）

```bash
# 1.1 备份配置文件（带时间戳）
sudo cp /home/admin/.openclaw/openclaw.json \
  /home/admin/.openclaw/openclaw.json.backup.20260403.2350

# 1.2 备份工作空间（排除 extensions）
tar -czf /tmp/openclaw-backup-20260403.tar.gz \
  --exclude='extensions' \
  -C /home/admin/.openclaw .

# 1.3 单独备份 workspace-chief-agent
tar -czf /tmp/workspace-backup-20260403.tar.gz \
  -C /home/admin/.openclaw workspace-chief-agent

# 1.4 验证备份
echo "=== 备份文件列表 ==="
ls -lh /tmp/openclaw-backup-20260403.tar.gz
ls -lh /tmp/workspace-backup-20260403.tar.gz
ls -lh /home/admin/.openclaw/openclaw.json.backup.20260403.2350

# 1.5 验证备份完整性
tar -tzf /tmp/openclaw-backup-20260403.tar.gz | head -20
tar -tzf /tmp/workspace-backup-20260403.tar.gz | head -20
```

**预计耗时**: 3 分钟  
**备份大小**: 约 3MB（不含 extensions）

---

### 步骤 2: 修复文件权限（23:53-23:54）

```bash
# 2.1 修复 openclaw.json 所有权
sudo chown admin:admin /home/admin/.openclaw/openclaw.json
sudo chmod 644 /home/admin/.openclaw/openclaw.json

# 2.2 验证权限
ls -la /home/admin/.openclaw/openclaw.json

# 2.3 修复 .openclaw 目录权限（如需要）
sudo chown -R admin:admin /home/admin/.openclaw/
```

**预计耗时**: 1 分钟

---

### 步骤 3: 升级 OpenClaw（23:54-23:58）

```bash
# 3.1 记录当前版本
echo "升级前版本:"
openclaw --version

# 3.2 升级到目标版本
sudo npm install -g openclaw@2026.3.28

# 3.3 验证升级
echo "升级后版本:"
openclaw --version

# 3.4 检查 npm 缓存
npm cache verify
```

**预计耗时**: 4 分钟  
**预期输出**: `OpenClaw 2026.3.28`

---

### 步骤 4: 配置检查（23:58-23:59）

```bash
# 4.1 检查配置加载
cat /home/admin/.openclaw/openclaw.json | grep '"lastTouchedVersion"'

# 4.2 检查插件配置
cat /home/admin/.openclaw/openclaw.json | grep -A 5 '"plugins"'

# 4.3 检查渠道配置
cat /home/admin/.openclaw/openclaw.json | grep -A 3 '"channels"'
```

**预计耗时**: 1 分钟

---

### 步骤 5: 重启 Gateway（23:59-00:01）

```bash
# 5.1 停止当前 Gateway
echo "正在停止 Gateway..."
openclaw gateway stop 2>&1 || sudo systemctl stop openclaw 2>&1 || echo "Gateway 已停止"

# 5.2 等待进程完全退出
sleep 3

# 5.3 确认已停止
pgrep -f openclaw || echo "Gateway 进程已停止"

# 5.4 启动新版本 Gateway
echo "正在启动 Gateway..."
openclaw gateway start 2>&1 || sudo systemctl start openclaw 2>&1

# 5.5 等待启动完成
sleep 5

# 5.6 检查 Gateway 状态
openclaw gateway status 2>&1 | head -30
```

**预计耗时**: 2 分钟

---

### 步骤 6: 功能验证（00:01-00:10）

```bash
# 6.1 验证版本号
echo "=== 版本验证 ==="
openclaw --version

# 6.2 验证插件列表
echo "=== 插件列表 ==="
ls -la /home/admin/.openclaw/extensions/

# 6.3 验证工作空间
echo "=== 工作空间 ==="
ls -la /home/admin/.openclaw/workspace-chief-agent/ | head -15

# 6.4 验证记忆数据库
echo "=== 记忆系统 ==="
ls -la /home/admin/.openclaw/memory/

# 6.5 验证配置文件
echo "=== 配置验证 ==="
cat /home/admin/.openclaw/openclaw.json | python3 -m json.tool > /dev/null && echo "✅ JSON 格式正确" || echo "❌ JSON 格式错误"

# 6.6 检查 Gateway 进程
echo "=== Gateway 进程 ==="
pgrep -a openclaw || echo "⚠️ Gateway 进程未运行"

# 6.7 检查端口监听
echo "=== 端口监听 ==="
netstat -tlnp 2>/dev/null | grep 16023 || ss -tlnp | grep 16023 || echo "⚠️ 端口 16023 未监听"
```

**预计耗时**: 5 分钟

---

## ✅ 验证清单

### 基础验证（必须全部通过）

- [ ] ✅ openclaw --version 显示 2026.3.28
- [ ] ✅ openclaw gateway status 显示正常运行
- [ ] ✅ openclaw.json 格式正确（JSON 验证通过）
- [ ] ✅ Gateway 进程运行中
- [ ] ✅ 端口 16023 正常监听

### 插件验证

- [ ] ✅ dingtalk-connector 存在且正常
- [ ] ✅ openclaw-qqbot 存在且正常
- [ ] ✅ dashscope-cfg 存在且正常

### 数据验证

- [ ] ✅ workspace-chief-agent 文件完整
- [ ] ✅ memory 目录文件完整
- [ ] ✅ chief-agent.sqlite 存在
- [ ] ✅ 配置文件中的钉钉配置完整

### 业务验证（次日验证）

- [ ] ⏳ 钉钉消息收发正常
- [ ] ⏳ QQ 机器人功能正常
- [ ] ⏳ 模型调用正常（DashScope）
- [ ] ⏳ Agent 功能正常

---

## 🔄 回滚方案（如需）

### 触发条件

出现以下任一情况立即回滚：
- Gateway 无法启动
- 配置文件加载失败
- 核心插件无法加载
- 业务功能严重异常

### 回滚步骤

```bash
# 1. 停止 Gateway
openclaw gateway stop 2>&1 || sudo systemctl stop openclaw 2>&1
sleep 3

# 2. 恢复配置文件
sudo cp /home/admin/.openclaw/openclaw.json.backup.20260403.2350 \
  /home/admin/.openclaw/openclaw.json

# 3. 回滚版本
sudo npm install -g openclaw@2026.3.13

# 4. 验证版本
openclaw --version  # 应显示 2026.3.13

# 5. 重启 Gateway
openclaw gateway start 2>&1 || sudo systemctl start openclaw 2>&1
sleep 5

# 6. 验证状态
openclaw gateway status
```

**回滚耗时**: 5-8 分钟

---

## 📊 升级时间线

| 时间 | 步骤 | 状态 |
|------|------|------|
| 23:45 | 升级前检查 | ⏳ 待执行 |
| 23:50 | 开始备份 | ⏳ 待执行 |
| 23:53 | 修复权限 | ⏳ 待执行 |
| 23:54 | 开始升级 | ⏳ 待执行 |
| 23:58 | 配置检查 | ⏳ 待执行 |
| 23:59 | 重启 Gateway | ⏳ 待执行 |
| 00:01 | 功能验证 | ⏳ 待执行 |
| 00:10 | 升级完成 | ⏳ 待执行 |

---

## 📞 应急联系

### 问题排查命令

```bash
# 查看 Gateway 日志
openclaw logs --tail 100

# 查看系统日志
journalctl -u openclaw -n 50 2>/dev/null || tail -50 /home/admin/.openclaw/logs/*.log

# 检查配置错误
cat /home/admin/.openclaw/openclaw.json | python3 -m json.tool

# 测试插件
openclaw plugins list 2>&1
```

### 关键文件位置

| 文件 | 位置 |
|------|------|
| 配置文件 | `/home/admin/.openclaw/openclaw.json` |
| 备份文件 | `/home/admin/.openclaw/openclaw.json.backup.20260403.2350` |
| Gateway 日志 | `/home/admin/.openclaw/logs/` |
| 工作空间 | `/home/admin/.openclaw/workspace-chief-agent/` |

---

## 🎯 成功标准

升级成功的标志：

1. ✅ 版本号显示 2026.3.28
2. ✅ Gateway 正常运行
3. ✅ 所有插件加载成功
4. ✅ 配置文件无错误
5. ✅ 工作空间文件完整
6. ✅ 记忆数据完整
7. ✅ 钉钉功能正常
8. ✅ QQ 功能正常

---

## 📝 升级记录

**升级开始时间**: ________  
**升级完成时间**: ________  
**实际耗时**: ________  
**执行者**: ________  

**遇到的问题**:
```
（如有问题，记录在此）
```

**解决方案**:
```
（如有问题，记录解决方案）
```

**验证结果**:
- 基础验证：✅ / ❌
- 插件验证：✅ / ❌
- 数据验证：✅ / ❌
- 业务验证：✅ / ❌

**总体评价**: 成功 / 部分成功 / 失败

---

**文档创建时间**: 2026-04-03 14:56 GMT+8  
**维护者**: 强国小马（chief-agent）
